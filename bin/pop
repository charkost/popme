#!/usr/bin/env ruby
require "popme/cli"
require "popme/storage"
commands = %[add list -l help -h]
if ARGV[0]
  if commands.include?(ARGV[0])
    Popme::CLI.start(ARGV)
  else
    Popme::CLI.start("open",ARGV[0])
  end
else
  Popme::CLI.help
end
